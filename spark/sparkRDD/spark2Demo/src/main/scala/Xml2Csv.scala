import org.apache.log4j.{Level, Logger}

//将原始数据xml转换为csv格式
object Xml2Csv {
  def main(args: Array[String]): Unit = {
    Logger.getLogger("org").setLevel(Level.ERROR)
    val strAppName = "xml2Csv"

  }
}
